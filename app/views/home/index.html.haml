%div
  %p To find repositories's list of author, type in nickname:
  = form_with url: "/search", method: :get do |form|
    = form.label :query, "Search for:"
    = form.text_field :name, placeholder: "enter user login"
    = form.submit "Search"
  %div
